export const symData = [
  {
    NSE: 1,
    BSE: 2,
    "NSE INDICES": 1,
    "BSE INDICES": 2,
    "All INDICES": 0,
    // "FNO": "",
  },
];

export const styles = {
  scrollable: {
    maxHeight: "66vh",
    overflowY: "scroll",
    margin: "20px 0",
  },
  separator: {
    height: "1px",
    backgroundColor: "#ccc",
    border: "none",
    margin: "5px 0",
  },
};


{/* <div
              className="icon"
              style={{
                color:
                  item && item.change < 0
                    ? "#F54040"
                    : item.change == 0
                    ? "#0000FF"
                    : "#007162",
                backgroundColor:
                  item && item.change < 0
                    ? "#F7E5DF"
                    : item.change == 0
                    ? "#d4ebf2"
                    : "#E6F4EA",
                display: `${isMobile ? "none" : "block"}`,
              }}
            >
              <span className="arrowIcon">
                {item?.change > 0 ? <ArrowUpwardIcon /> : <ArrowDownwardIcon />}
              </span>
            </div> */}
